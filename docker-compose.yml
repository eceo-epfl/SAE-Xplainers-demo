services:
  umap-demo:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - umap-demo-node-modules:/app/node_modules
    command: npm run dev -- --host 0.0.0.0 --port 5173
    stdin_open: true
    tty: true

volumes:
  umap-demo-node-modules:
